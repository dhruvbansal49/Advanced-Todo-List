<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manager</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/todos.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" integrity="sha512-+4zCK9k+qNFUR5X+cKL9EIR+ZOhtIloNl9GIKS57V1MyNsYpYcUrUeQc9vNfzsWfV28IaLL3i96P9sdNyeRssA==" crossorigin="anonymous" />
    
    <script src="https://code.jquery.com/jquery-2.1.4.min.js" integrity="sha256-8WqyJLuWKRBVhxXIL1jBDD7SDxU936oZkCnxQbWwJVw=" crossorigin="anonymous"></script>
</head>
<body>
    <nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">EnR Consultancy Services</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav ms-auto">
                <% if(!signedUser){ %>
                <a class="nav-link" href="/manager/register">Register</a>
                <% }else{ %> 
                <a class="nav-link" href="/manager/<%=manager._id%>/project">Add Project</a>
                <a class="nav-link" href="/manager/<%=manager._id%>">Home</a>
                <a class="nav-link" href="/manager/logout">Logout</a> 
                <% } %> 
            </div>
          </div>
        </div>
    </nav>
    <%- include('../../partials/flash') %> 
    <% for(let project of manager.projects){ %> 
    <div id ="container">
        <div class="setColor"><h1><%= project.title %><span style="float: right;"><form action="/manager/<%=manager._id%>/project/<%= project._id %>?_method=DELETE" method="POST">
            <button type="submit" class="delet float-right"><i class="fas fa-trash-alt"></i></button>  
        </form></span></h1></div>
        <form action="/manager/<%=manager._id%>/project/<%= project._id %>?_method=PUT" method="POST">
            <input type="text" placeholder="Add Employee to Execute The Project" name="username">
            <div class="d-grid">
                <button class="btn btn-info">Add</button>
            </div>
        </form>
        <ul>
            <% for(let emp of project.employees){ %>
                <li style="padding-left: 20px;">
                    <%= emp.name %>
                    <span style="float: right;"><a href="/manager/<%=manager._id%>/project/<%=project._id%>/employee/<%=emp._id%>" class="btn btn-sm btn-success">Add/Review Task</a></span>
                    <span style="float: right;"><form action="/manager/<%=manager._id%>/project/<%= project._id %>/employee/<%=emp._id%>?_method=DELETE" method="POST">
                        <button type="submit" class="btn btn-sm btn-danger">Remove Employee</button>  
                    </form></span>
                </li>
            <% } %> 
        </ul>  
    </div>
    <% } %> 
    <script src="/javascripts/todos.js"></script>
</body>
</html>